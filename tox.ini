[tox]
envlist =
    py{38,39,310,311}-plone60,
    py{311}-plone61,
    py{311}-plone62,
skip_missing_interpreters = True

[coverage:run]
relative_files = True

[testenv]
skip_install = true
extras =
    test
commands_pre =
    {envbindir}/buildout -c {toxinidir}/{env:version_file} buildout:directory={envdir} buildout:develop={toxinidir} install test
commands =
    {envbindir}/coverage debug sys
    {envbindir}/coverage run {envbindir}/test
    {envbindir}/coverage html
#   TODO: increase coverage
#   {envbindir}/coverage report -m --fail-under=85
    {envbindir}/coverage report -m --fail-under=40
    {envbindir}/coverage json -i
setenv =
    plone60: version_file=test-6.0.x.cfg
    plone61: version_file=test-6.1.x.cfg
    plone62: version_file=test-6.2.x.cfg
deps =
    plone60: -rrequirements.txt
    plone61: -rrequirements_61.txt
    plone62: -rrequirements_62.txt
    coverage==7.14.3
